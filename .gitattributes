# Auto-detect text files, use LF everywhere
* text=auto eol=lf

# Go source
*.go text diff=golang

# Markdown
*.md text diff=markdown

# YAML
*.yml text
*.yaml text

# Lock files — binary
go.sum -diff
go.mod -diff

# Binaries — don't attempt to diff
*.exe binary
*.test binary
